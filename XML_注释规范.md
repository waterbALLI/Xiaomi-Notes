# XML 文件注释规范

## 注释格式

每个 XML 文件末尾添加如下格式的注释：

```xml
<!-- 
================================================================================
文件注释说明
================================================================================

文件路径: [文件相对路径]
文件类型: [color/drawable/layout/menu/values/xml]
创建时间: [YYYY-MM-DD]
最后修改: [YYYY-MM-DD]

功能描述:
[简要描述文件的主要功能和作用]

主要元素说明:
1. [元素名称]: [功能说明]
2. [元素名称]: [功能说明]
3. [元素名称]: [功能说明]

使用场景:
[描述在哪些场景下使用此文件]

注意事项:
[使用时的注意事项或特殊配置]

相关文件:
- [相关文件1]
- [相关文件2]

================================================================================
-->
```

## 注释内容要求

### 1. 文件路径
- 使用相对于 `res` 文件夹的相对路径
- 示例: `color/primary_text_dark.xml`

### 2. 文件类型
- `color`: 颜色定义文件
- `drawable`: 图形资源文件
- `layout`: 布局文件
- `menu`: 菜单文件
- `values`: 值资源文件（strings、colors、dimens等）
- `xml`: 其他 XML 配置文件

### 3. 功能描述
- 简要说明文件的主要用途
- 不超过 3 句话

### 4. 主要元素说明
- 列出文件中重要的元素及其功能
- 每个元素一行，格式: `元素名称: 功能说明`

### 5. 使用场景
- 描述在应用程序的哪些部分会使用到此文件
- 可以说明对应的 Activity、Fragment 或功能模块

### 6. 注意事项
- 特殊配置要求
- 兼容性注意事项
- 性能优化建议

### 7. 相关文件
- 列出与此文件相关的其他文件
- 包括布局文件、Java/Kotlin 代码文件等

## 示例

```xml
<!-- 
================================================================================
文件注释说明
================================================================================

文件路径: layout/note_edit.xml
文件类型: layout
创建时间: 2023-10-15
最后修改: 2024-03-20

功能描述:
笔记编辑界面的主布局文件，包含标题输入框、内容编辑区域和操作按钮。

主要元素说明:
1. EditText (note_title): 笔记标题输入框
2. EditText (note_content): 笔记内容编辑区域
3. Button (save_button): 保存按钮
4. Button (cancel_button): 取消按钮

使用场景:
在用户创建新笔记或编辑现有笔记时显示此界面。

注意事项:
1. 内容编辑区域支持多行文本
2. 标题输入框有最大长度限制
3. 保存按钮在内容为空时禁用

相关文件:
- NoteEditActivity.java
- note_item.xml
- strings.xml (相关文本资源)

================================================================================
-->
```

## 文件夹结构说明

### color/
- 颜色资源定义文件
- 用于定义应用程序的主题颜色

### drawable/
- 图形资源文件
- 包括图标、背景、选择器等

### layout/
- 界面布局文件
- 定义 Activity、Fragment、Dialog 等的界面结构

### menu/
- 菜单资源文件
- 定义选项菜单、上下文菜单等

### values/
- 值资源文件
- 包括字符串、颜色、尺寸、样式等定义

### xml/
- 其他 XML 配置文件
- 包括偏好设置、搜索配置、小部件配置等

## 更新维护

1. 每次修改 XML 文件时，更新"最后修改"日期
2. 如果功能有重大变化，更新功能描述
3. 添加新元素时，更新主要元素说明
4. 删除元素时，从注释中移除对应说明